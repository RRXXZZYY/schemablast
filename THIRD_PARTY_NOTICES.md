# Third-party notices

SchemaBlast contains original code and has no runtime dependencies. It does not
bundle a schema registry, catalog, SQL parser, orchestration system, or vendor
SDK. Its small contract format uses generic data-engineering concepts.
