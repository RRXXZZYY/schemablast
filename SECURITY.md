# Security policy

Report vulnerabilities through GitHub private vulnerability reporting. Include
the affected version and a synthetic snapshot pair. Do not attach confidential
catalog exports or credentials to a public issue.

SchemaBlast validates graph bounds and node references before traversal, makes
no network calls, does not execute SQL or transforms, and escapes all snapshot
text in HTML output.
